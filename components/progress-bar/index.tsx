import React from 'react'

type ProgressBarTypes = {
	className?: string
	children?: React.ReactNode
}

export const ProgressBar = ({className, children}: ProgressBarTypes ) => {
	// let progress = useProgress();
	// let width = useMotionTemplate`${progress}%`;
  return (
	<div>index</div>
  )
}
