/** @type {import('next').NextConfig} */
const nextConfig = {
	// experimental: {
	// 	typedRoutes: true,
	// },
	// output: 'standalone',
	// reactStrictMode: true,
	// cssModules: true,
}

module.exports = nextConfig
